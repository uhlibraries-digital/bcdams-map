{
  "@type": "Property",
  "@id": "{{ site.url }}#{{ element.name }}",
  "label": "{{ element.label }}",
  "comment": "{{ element.definition }}"
  {% if element.source %},"sameAs": "{{ element.source }}"{% endif %}
}
